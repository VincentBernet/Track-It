import styled from 'styled-components';

const StyledListReset = styled.ul`
  list-style: none;
  margin: 0;
  padding: 0;

  
  display: flex;
  flex-direction: column;
  gap: 15px;
`;

export default StyledListReset;